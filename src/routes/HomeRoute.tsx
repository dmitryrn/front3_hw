import EmptyState from '../components/ui/EmptyState/EmptyState'

export default function HomeRoute() {
  return <EmptyState title="Выберите чат" text="Откройте существующий чат слева или создайте новый." />
}
